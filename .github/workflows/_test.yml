name: _test

on:
  push:
    branches: [prototype]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from GitHub Actions"
      - run: date
      - run: uname -a
