# OS / editor cruft
.DS_Store
Thumbs.db
*.log
*.zip

# Secrets (never commit)
secrets.env
secrets.env.local
.env

# Build artifacts
dist/
_site/

# Legacy Python bridge artifacts (kept for ref; the bridge is removed from this repo)
__pycache__/
*.pyc
.venv/
venv/

# Smile profile (per-host local state)
smile-profile/
active.json

# Node modules (if anyone runs html-validate locally)
node_modules/
